// 设置页骨架，后续接入主题与语言配置
export function Settings() {
  return (
    <main className="flex min-h-screen items-center justify-center px-6">
      <section className="w-full max-w-3xl rounded-xl2 border border-line bg-surface-panel p-8 shadow-panel">
        <h1 className="text-2xl font-semibold text-ink">设置页骨架</h1>
        <p className="mt-3 text-sm text-ink-soft">阶段一后续会接入语言、主题和并发配置。</p>
      </section>
    </main>
  )
}
