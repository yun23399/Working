# 多智能体编排平台 — 完整架构与开发引导文档

> 版本：v1.0 | 语言：中文 | 更新时间：2026-05-12

---

## 目录

1. [技术选型与推荐理由](#一技术选型与推荐理由)
2. [完整项目目录结构](#二完整项目目录结构)
3. [核心模块说明](#三核心模块说明)
4. [数据库结构设计](#四数据库结构设计)
5. [分阶段开发路线图](#五分阶段开发路线图)
6. [Codex 开发引导 Prompt](#六codex-开发引导-prompt)
7. [AGENTS.md 规范文件（完整内容）](#七agentsmd-规范文件完整内容)
8. [PROGRESS.md 进度文件模板](#八progressmd-进度文件模板)

---

## 一、技术选型与推荐理由

### 前端

| 技术 | 版本 | 选用理由 |
|------|------|----------|
| React | 18 | 成熟生态、并发渲染、流式 UI 天然支持 |
| TypeScript | 5.x | 类型安全，Codex 生成代码质量更高 |
| Vite | 5.x | 极速热更新，本地开发体验优秀 |
| TailwindCSS | 3.x | 原子类减少样式冲突，主题切换简单 |
| Zustand | 4.x | 轻量状态管理，无需 Redux 样板代码 |
| React Query | 5.x | 服务端数据缓存与同步 |
| Framer Motion | 11.x | 高质量页面过渡与元素动效 |
| Monaco Editor | — | 代码预览与语法高亮（VS Code 同款） |
| i18next | — | 中英文国际化 |

### 后端

| 技术 | 版本 | 选用理由 |
|------|------|----------|
| Python | 3.11+ | AI 生态最完善，LLM 库全面 |
| FastAPI | 0.110+ | 高性能异步，WebSocket 原生支持，自动 API 文档 |
| SQLAlchemy | 2.x | ORM，SQLite/PostgreSQL 无缝切换 |
| Alembic | — | 数据库迁移管理 |
| LiteLLM | — | 统一接入 OpenAI / Anthropic / Ollama 等 |
| Playwright | — | 浏览器自动化 |
| loguru | — | 优雅的分级日志系统 |
| httpx | — | 异步 HTTP 客户端 |
| Pydantic | 2.x | 数据校验与序列化 |
| python-jose | — | JWT 认证 |
| bcrypt | — | 密码加密 |

### 工程规范

| 工具 | 用途 |
|------|------|
| ESLint + Prettier | 前端代码规范与格式化 |
| Ruff + Black | 后端代码规范与格式化 |
| Husky + lint-staged | 提交前自动检查 |
| GitHub Actions | CI：推送后自动跑 Lint |
| Gitmoji | Commit 消息风格（✨ 🐛 📝 等） |
| Conventional structure | 模块边界清晰，责任单一 |

---

## 二、完整项目目录结构

```
multi-agent-platform/
├── AGENTS.md                    # Codex 规范文件（静态，不随开发变动）
├── PROGRESS.md                  # 开发进度文件（每次执行后更新）
├── README.md                    # 项目启动与配置说明
├── CHANGELOG.md                 # 版本变更记录
├── .env.example                 # 环境变量示例（不含真实 Key）
├── .env                         # 真实环境变量（gitignore）
├── .gitignore
├── .github/
│   └── workflows/
│       └── ci.yml               # GitHub Actions CI 配置
│
├── frontend/                    # 前端 React 应用
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── tailwind.config.ts
│   ├── .eslintrc.cjs
│   ├── .prettierrc
│   ├── index.html
│   └── src/
│       ├── main.tsx
│       ├── App.tsx
│       ├── i18n/                # 国际化
│       │   ├── zh.json
│       │   └── en.json
│       ├── assets/              # 静态资源
│       ├── components/          # 通用 UI 组件
│       │   ├── ui/              # 基础组件（Button、Input 等）
│       │   ├── chat/            # 对话相关组件
│       │   │   ├── ChatWindow.tsx
│       │   │   ├── MessageBubble.tsx
│       │   │   ├── StreamingText.tsx
│       │   │   ├── FileUploadZone.tsx
│       │   │   └── TokenCounter.tsx
│       │   ├── workspace/       # 产出物预览
│       │   │   ├── PreviewPanel.tsx
│       │   │   ├── CodePreview.tsx
│       │   │   ├── ImagePreview.tsx
│       │   │   └── DocumentPreview.tsx
│       │   ├── workflow/        # 工作流展示
│       │   │   ├── WorkflowConfirm.tsx
│       │   │   ├── AgentCard.tsx
│       │   │   └── LogViewer.tsx
│       │   └── layout/          # 布局组件
│       │       ├── TopNav.tsx
│       │       ├── Sidebar.tsx
│       │       └── MainArea.tsx
│       ├── pages/               # 页面级组件
│       │   ├── Chat.tsx
│       │   ├── Projects.tsx
│       │   ├── Settings.tsx
│       │   └── Login.tsx
│       ├── stores/              # Zustand 状态管理
│       │   ├── authStore.ts
│       │   ├── projectStore.ts
│       │   ├── chatStore.ts
│       │   ├── workflowStore.ts
│       │   └── settingsStore.ts
│       ├── hooks/               # 自定义 React Hooks
│       │   ├── useWebSocket.ts
│       │   ├── useStream.ts
│       │   └── useTheme.ts
│       ├── api/                 # API 请求封装
│       │   ├── client.ts        # axios/fetch 基础配置
│       │   ├── auth.ts
│       │   ├── projects.ts
│       │   ├── conversations.ts
│       │   └── workflows.ts
│       ├── types/               # TypeScript 类型定义
│       │   ├── agent.ts
│       │   ├── workflow.ts
│       │   ├── message.ts
│       │   └── project.ts
│       └── utils/               # 工具函数
│           ├── format.ts
│           ├── export.ts
│           └── notify.ts
│
├── backend/                     # 后端 FastAPI 应用
│   ├── requirements.txt
│   ├── requirements-dev.txt
│   ├── pyproject.toml           # Ruff / Black 配置
│   ├── alembic.ini
│   ├── alembic/                 # 数据库迁移文件
│   │   ├── env.py
│   │   └── versions/
│   └── app/
│       ├── main.py              # FastAPI 应用入口
│       ├── config.py            # 全局配置（读取 .env）
│       ├── database.py          # 数据库连接与 Session
│       │
│       ├── api/                 # API 路由层
│       │   ├── __init__.py
│       │   ├── deps.py          # 公共依赖（认证校验等）
│       │   ├── auth.py          # 登录注册接口
│       │   ├── projects.py      # 项目管理接口
│       │   ├── conversations.py # 对话接口
│       │   ├── workflows.py     # 工作流接口
│       │   ├── agents.py        # Agent 配置接口
│       │   └── ws.py            # WebSocket 端点
│       │
│       ├── models/              # SQLAlchemy 数据模型
│       │   ├── user.py
│       │   ├── project.py
│       │   ├── conversation.py
│       │   ├── message.py
│       │   ├── workflow.py
│       │   ├── agent_config.py
│       │   └── artifact.py
│       │
│       ├── schemas/             # Pydantic 请求/响应模型
│       │   ├── auth.py
│       │   ├── project.py
│       │   ├── conversation.py
│       │   ├── workflow.py
│       │   └── agent.py
│       │
│       ├── services/            # 业务逻辑层
│       │   ├── auth_service.py
│       │   ├── project_service.py
│       │   ├── conversation_service.py
│       │   └── artifact_service.py
│       │
│       ├── core/                # 核心智能层
│       │   ├── llm/
│       │   │   ├── adapter.py   # LiteLLM 统一适配器
│       │   │   ├── providers.py # 各 LLM 提供商配置
│       │   │   └── streaming.py # 流式输出处理
│       │   ├── manager/
│       │   │   ├── manager_agent.py     # Manager Agent 主逻辑
│       │   │   ├── requirement_extractor.py  # 需求结构化提取
│       │   │   └── workflow_planner.py  # 工作流规划生成
│       │   └── memory/
│       │       └── project_memory.py    # 项目级记忆管理
│       │
│       ├── workflow/            # 工作流引擎
│       │   ├── dag_orchestrator.py  # DAG 编排调度
│       │   ├── agent_spawner.py     # Agent 实例动态创建
│       │   ├── checkpoint.py        # 断点快照与恢复
│       │   ├── error_handler.py     # 错误处理与层级上报
│       │   └── workspace.py         # 共享工作区管理
│       │
│       ├── agents/              # Agent 运行时
│       │   ├── base_agent.py    # Agent 基类
│       │   ├── agent_runner.py  # Agent 执行循环
│       │   └── templates/       # 预置角色模板
│       │       ├── frontend_dev.py
│       │       ├── backend_dev.py
│       │       ├── tester.py
│       │       ├── product_manager.py
│       │       └── designer.py
│       │
│       ├── tools/               # Agent 工具集
│       │   ├── base_tool.py     # 工具基类接口
│       │   ├── code_executor.py # 代码执行（subprocess / Docker）
│       │   ├── browser_tool.py  # Playwright 浏览器自动化
│       │   ├── api_caller.py    # 外部 API 调用
│       │   ├── file_tool.py     # 文件读写操作
│       │   └── image_tool.py    # 图像生成与截图
│       │
│       ├── notifications/       # 通知系统
│       │   └── notifier.py      # 系统托盘 + 声音通知
│       │
│       └── utils/               # 工具函数
│           ├── logger.py        # loguru 日志配置
│           ├── security.py      # JWT / 密码工具
│           └── file_export.py   # 产出物导出工具
│
├── plugins/                     # 插件目录
│   ├── README.md                # 插件开发规范
│   └── examples/                # 示例插件
│
├── workspace/                   # 运行时工作区（gitignore）
│   └── projects/                # 各项目产出物
│
├── logs/                        # 本地日志文件（gitignore）
│
└── docs/                        # 模块详细文档
    ├── architecture.md
    ├── llm-adapter.md
    ├── workflow-engine.md
    ├── agent-system.md
    ├── tools.md
    ├── plugin-guide.md
    └── api-reference.md
```

---

## 三、核心模块说明

### 3.1 Manager Agent 工作流程

```
用户输入需求
    ↓
Manager Agent 多轮对话收集需求
    ↓
需求提取器 → 结构化 JSON（目标/约束/产出物）
    ↓
工作流规划器 → 生成 DAG（角色列表 + 步骤 + 顺序）
    ↓
[可配置] 前端展示工作流预览，等待用户确认
    ↓
DAG 编排器开始执行
    ↓
Agent 生成器动态创建各角色实例
    ↓
各 Agent 串行/并行执行任务 → 实时流式输出到前端
    ↓
任务完成 → 产出物导出到本地 + 系统通知
```

### 3.2 错误处理流程

```
Agent 执行失败
    ↓
重试（按该 Agent 配置的次数）
    ↓ 仍失败
记录详细日志（错误类型、上下文、调用栈）
    ↓
回滚到上一个检查点快照
    ↓
向上级 Agent 上报（由当前工作流的层级定义决定）
    ↓
上级 Agent 分析失败原因 → 给出修改建议
    ↓
用户可选择：接受建议重新执行 / 手动修改 / 终止
```

### 3.3 LLM 适配层接口规范

```python
# 所有 LLM 调用统一通过此接口，屏蔽底层差异
class LLMAdapter:
    async def chat(
        self,
        messages: list[dict],
        model: str,          # 如 "gpt-4o" / "claude-3-5-sonnet" / "ollama/llama3"
        stream: bool = True,
        tools: list = None,  # Function calling 工具定义
    ) -> AsyncGenerator[str, None]: ...
```

### 3.4 Agent 基类接口规范

```python
class BaseAgent:
    role: str              # 角色名称（如 "前端开发者"）
    system_prompt: str     # 角色 Prompt
    llm_model: str         # 该 Agent 使用的 LLM
    tools: list[BaseTool]  # 该 Agent 可用的工具
    max_retries: int       # 最大重试次数

    async def run(self, task: AgentTask) -> AgentResult: ...
    async def report_error(self, error: AgentError) -> None: ...
```

---

## 四、数据库结构设计

### 核心数据表

```sql
-- 用户表
users (id, username, password_hash, created_at, settings_json)

-- 项目表
projects (id, user_id, name, description, created_at, updated_at)

-- 对话表
conversations (id, project_id, title, created_at, updated_at)

-- 消息表
messages (id, conversation_id, role, content, token_count, created_at)

-- 工作流定义表
workflows (id, conversation_id, dag_json, status, created_at)

-- 工作流执行记录表
workflow_runs (id, workflow_id, status, checkpoint_json, started_at, finished_at)

-- Agent 配置表
agent_configs (id, user_id, name, role, system_prompt, llm_model, tools_json, max_retries)

-- 产出物表
artifacts (id, workflow_run_id, type, name, content_path, preview_data, created_at)

-- 日志表
agent_logs (id, workflow_run_id, agent_id, level, message, metadata_json, created_at)
```

---

## 五、分阶段开发路线图

### 阶段一：基础骨架 MVP（建议 2-3 周）

**目标**：先让仓库真实可运行，再打通登录、对话、流式回复、历史持久化闭环

- [ ] 仓库状态对齐（先确认 `PROGRESS.md` 与实际文件树一致）
- [ ] 项目脚手架真实落地（前后端目录结构、配置文件、`.env.example`、CI）
- [ ] 前后端依赖安装与验证（lint / build / migration / startup）
- [ ] 用户注册/登录（密码加密 + JWT）
- [ ] 最小对话闭环（创建/进入对话、发送消息、WebSocket 流式回复、消息持久化）
- [ ] LLM 适配层（优先支持 OpenAI + Ollama，Anthropic 可按需补齐）
- [ ] Manager Agent 基础对话收集需求
- [ ] 最小项目/对话历史边栏
- [ ] 亮/暗主题切换

**阶段一完成标志**：当前仓库真实包含前后端工程；用户可登录，与 Manager Agent 对话，看到实时流式回复，历史记录持久化，且阶段一验证命令全部通过

---

### 阶段二：工作流引擎（建议 3-4 周）

**目标**：Manager 能规划工作流，多 Agent 协作执行任务

- [ ] 需求提取器（LLM function calling 结构化需求）
- [ ] 工作流规划器（生成 DAG JSON）
- [ ] 工作流预览确认界面
- [ ] DAG 编排器（串行/并行调度）
- [ ] Agent 生成器（动态创建角色实例）
- [ ] 预置角色模板（前端/后端/测试/PM/设计师）
- [ ] 共享工作区（SQLite 运行时存储）
- [ ] 断点控制（暂停/恢复/中断）
- [ ] 错误处理与层级上报
- [ ] 工作流执行日志实时展示

**阶段二完成标志**：说"帮我做个网页"，Manager 规划工作流，前端/后端 Agent 协作生成代码

---

### 阶段三：工具集接入（建议 2-3 周）

**目标**：Agent 具备实际执行能力

- [ ] 代码执行工具（subprocess，沙盒开关）
- [ ] 文件读写工具（创建/修改/导出到本地目录）
- [ ] 外部 API 调用工具
- [ ] Playwright 浏览器自动化工具
- [ ] 图像生成工具（DALL·E API + 截图）
- [ ] 产出物内嵌预览（代码高亮 + HTML 渲染 + 图片）
- [ ] 产出物导出到本地目录
- [ ] Token 用量实时展示

**阶段三完成标志**：Agent 能写文件、执行代码、爬取网页、生成图片，产出物可预览和下载

---

### 阶段四：完善体验（建议 2 周）

**目标**：打磨细节，提升使用体验

- [ ] 系统通知（托盘提醒 + 声音）
- [ ] 多项目工作区管理（创建/切换/搜索）
- [ ] 对话内内容搜索
- [ ] 用户自定义 Agent 角色（增删改）
- [ ] Agent 配置导入/导出（JSON 文件）
- [ ] 并发工作流数量上限配置
- [ ] 中英文切换完善
- [ ] 页面过渡动效优化（Framer Motion）
- [ ] 日志面板（分级过滤 + 写入本地文件）

---

### 阶段五：扩展能力（建议持续迭代）

- [ ] 插件市场（本地插件加载机制）
- [ ] Agent 配置云同步
- [ ] PostgreSQL 支持（连接字符串切换）
- [ ] OAuth 登录（Google / GitHub）
- [ ] Docker 一键启动支持
- [ ] Electron 桌面应用打包

---

## 六、Codex 开发引导 Prompt

> 以下 Prompt 用于向 Codex 下达开发任务。每次新建 Codex 会话时，将当前 `AGENTS.md` 和 `PROGRESS.md` 的内容附在 Prompt 前。

---

### 通用系统 Prompt（每次必带）

```
你是这个项目的核心开发者。在执行任何任务之前，你必须：

1. 阅读项目根目录的 AGENTS.md，理解代码规范、目录结构、命名约定和禁止事项
2. 阅读项目根目录的 PROGRESS.md，了解当前开发阶段和已完成内容
3. 核对当前仓库实际状态是否与 PROGRESS.md 一致；若不一致，先修正 PROGRESS.md 与目录快照
4. 确认本次任务属于 PROGRESS.md 中记录的当前阶段范围内
5. 开始执行任务
6. 任务完成后，立即更新 PROGRESS.md（当前阶段、已完成项、操作日志）

严格遵守 AGENTS.md 中的所有规范，不允许跳过或违反任何一条。
```

---

### 阶段一启动 Prompt

```
【当前阶段】阶段一：基础骨架 MVP

【本次任务】完成仓库状态对齐，并落地阶段一最小可运行闭环的第一轮实现

请按照以下要求执行：

执行前：
- 先检查当前仓库是否真实存在 `frontend/`、`backend/`、`README.md`、`.env.example`、`.gitignore`、`.github/workflows/ci.yml`
- 如果 `PROGRESS.md` 的完成项或目录快照与实际仓库不一致，先修正 `PROGRESS.md`
- 若工程代码不存在，先从当前仓库开始创建真实脚手架，不要沿用“脚手架已完成”的假设

前端部分：
- 在 frontend/ 目录使用 Vite + React + TypeScript 初始化项目
- 安装并配置 TailwindCSS、Zustand、React Query、Framer Motion、i18next
- 创建 AGENTS.md 中规定的完整目录结构
- 配置 ESLint + Prettier，规则见 AGENTS.md
- 创建基础路由（/login、/chat、/projects、/settings）
- 实现亮/暗主题切换（CSS 变量 + TailwindCSS dark mode）

后端部分：
- 在 backend/ 目录初始化 FastAPI 项目
- 安装 requirements.txt 中的所有依赖
- 配置 Ruff + Black，规则见 AGENTS.md
- 创建 AGENTS.md 中规定的完整目录结构
- 配置 SQLAlchemy + Alembic（SQLite 连接）
- 创建所有数据库模型（见架构文档数据库设计章节）
- 实现 /api/auth/register 和 /api/auth/login 接口

工程配置：
- 创建 .env.example（列出所有必要环境变量，不含真实值）
- 配置 .gitignore
- 配置 GitHub Actions CI（推送时运行 ESLint 和 Ruff 检查）
- 创建 README.md（本地启动步骤）
- 验证 `npm run lint`、`npm run build`、`ruff check .`、`black --check .`、`alembic upgrade head`、`uvicorn app.main:app`

完成后更新 PROGRESS.md。
```

---

### 单模块开发 Prompt 模板

```
【当前阶段】阶段 X：[阶段名称]
【本次任务】实现 [模块名称]

背景：[简要说明该模块的作用和上下文]

具体要求：
1. [要求 1]
2. [要求 2]
3. [要求 3]

接口约定：
- 输入：[输入格式]
- 输出：[输出格式]

注意事项：
- 必须遵守 AGENTS.md 中的命名规范
- 所有函数必须有中文注释说明用途
- 错误处理不能省略
- 完成后运行 Lint 确认无报错

完成后更新 PROGRESS.md，记录本次操作日志。
```

---

### WebSocket 流式输出任务 Prompt

```
【任务】实现 WebSocket 实时流式推送

后端要求（backend/app/api/ws.py）：
- 创建 WebSocket 端点 /ws/{conversation_id}
- 维护连接池（connection_manager.py）
- 支持以下消息类型推送：
  - token：LLM 生成的单个 token（流式打字效果）
  - agent_status：Agent 状态变更（等待/运行/完成/失败）
  - log：日志消息（含 level 字段）
  - artifact：产出物通知（含文件路径和类型）
  - workflow_update：工作流节点状态更新
- 断线时自动清理连接

前端要求（frontend/src/hooks/useWebSocket.ts）：
- 封装 WebSocket 连接管理（自动重连，最多 3 次）
- 按消息类型分发到对应的 Zustand store
- token 消息更新 chatStore 实现打字机效果
- log 消息更新 workflowStore 的日志列表

完成后更新 PROGRESS.md。
```

---

### 错误处理模块 Prompt

```
【任务】实现 Agent 错误处理与层级上报

文件：backend/app/workflow/error_handler.py

实现以下逻辑：
1. Agent 执行失败时，根据该 Agent 的 max_retries 配置重试
2. 重试耗尽后：
   a. 记录详细错误日志（loguru，ERROR 级别）
   b. 将当前工作区快照回滚到上一个检查点
   c. 根据当前工作流的动态层级定义，找到上级 Agent
   d. 向上级 Agent 发送错误报告（包含：失败 Agent 角色、任务描述、错误信息、建议方向）
   e. 上级 Agent 分析后生成修改建议
   f. 通过 WebSocket 将建议推送到前端，等待用户决策
3. 用户可选择：
   a. 接受建议 → 重新执行失败的 Agent
   b. 手动修改建议 → 执行修改后的方案
   c. 终止工作流

注意：层级结构不硬编码，从当前工作流的 dag_json 中动态读取上下级关系。

完成后更新 PROGRESS.md。
```

---

## 七、AGENTS.md 规范文件（完整内容）

> 将以下内容保存为项目根目录的 `AGENTS.md`，Codex 每次执行前必须读取此文件。

---

```markdown
# AGENTS.md — Codex 开发规范

> 本文件是 Codex 的强制规范文件。执行任何代码操作前必须先完整阅读本文件。
> 不允许跳过、违反或修改本文件中的任何规定。

---

## 0. 执行前必做事项

在开始任何任务之前，必须：
1. 完整阅读本文件（AGENTS.md）
2. 完整阅读 PROGRESS.md
3. 确认本次任务在 PROGRESS.md 标注的当前阶段范围内
4. 任务完成后立即更新 PROGRESS.md

---

## 1. 项目技术栈

### 前端
- React 18 + TypeScript 5 + Vite 5
- TailwindCSS 3（样式）
- Zustand 4（状态管理）
- React Query 5（数据请求）
- Framer Motion 11（动效）
- i18next（国际化）

### 后端
- Python 3.11+ / FastAPI / SQLAlchemy 2 / Pydantic 2
- LiteLLM（LLM 统一接口）
- Playwright（浏览器自动化）
- loguru（日志）

---

## 2. 目录结构规范

严格遵守 README.md 中定义的目录结构，不允许在规定目录之外创建文件。

- 前端组件放 frontend/src/components/
- 前端页面放 frontend/src/pages/
- 后端 API 路由放 backend/app/api/
- 后端业务逻辑放 backend/app/services/
- 核心 AI 逻辑放 backend/app/core/
- 工具集放 backend/app/tools/

---

## 3. 命名约定

### 前端（TypeScript）
- 组件文件：PascalCase（如 ChatWindow.tsx）
- Hook 文件：camelCase，以 use 开头（如 useWebSocket.ts）
- Store 文件：camelCase，以 Store 结尾（如 chatStore.ts）
- 工具函数：camelCase（如 formatDate.ts）
- 类型定义：PascalCase 接口（如 interface AgentConfig）
- CSS 类名：使用 Tailwind 原子类，自定义类用 kebab-case

### 后端（Python）
- 文件名：snake_case（如 agent_runner.py）
- 类名：PascalCase（如 BaseAgent）
- 函数名：snake_case（如 run_workflow）
- 常量：UPPER_SNAKE_CASE（如 MAX_RETRIES）
- Pydantic 模型：PascalCase + Schema 后缀（如 WorkflowCreateSchema）
- SQLAlchemy 模型：PascalCase（如 WorkflowRun）

---

## 4. 代码注释规范

- 所有注释使用中文
- 每个函数/方法必须有简短的中文注释说明用途
- 复杂逻辑块必须有行内注释
- 类必须有中文 docstring

示例（Python）：
\`\`\`python
class LLMAdapter:
    """LLM 统一适配器，屏蔽不同提供商的 API 差异"""

    async def chat(self, messages: list, model: str) -> AsyncGenerator:
        """统一的对话接口，支持流式输出"""
        # 根据 model 前缀判断使用哪个提供商
        ...
\`\`\`

示例（TypeScript）：
\`\`\`typescript
// 管理 WebSocket 连接，支持自动重连
const useWebSocket = (conversationId: string) => {
  ...
}
\`\`\`

---

## 5. 代码规范工具

### 前端
- ESLint：使用 @typescript-eslint 规则集
- Prettier：单引号、无分号、2 空格缩进、100 字符换行
- 提交前通过 Husky + lint-staged 自动检查

### 后端
- Ruff：启用 E、F、I、N 规则组
- Black：88 字符换行
- 提交前必须通过 ruff check . 和 black --check .

---

## 6. Git 规范

### 分支策略
- main：稳定版本，只接受来自 dev 的合并
- dev：开发分支，所有功能在此开发
- 不允许直接推送到 main

### Commit 消息格式（Gitmoji）
\`\`\`
<emoji> <类型>: <中文描述>

示例：
✨ feat: 实现 Manager Agent 需求收集功能
🐛 fix: 修复 WebSocket 断线不重连的问题
📝 docs: 更新 API 接口文档
♻️ refactor: 重构 DAG 编排器错误处理逻辑
🎨 style: 统一前端组件间距规范
⚡️ perf: 优化 Agent 并发执行性能
🔧 chore: 更新依赖版本
\`\`\`

---

## 7. 模块责任边界

### 前端模块
- components/：只负责 UI 渲染，不包含业务逻辑
- stores/：只管理状态，不直接调用 API
- api/：只负责 HTTP/WebSocket 请求，不处理业务逻辑
- hooks/：封装可复用的副作用逻辑
- pages/：组合组件与 store，处理页面级逻辑

### 后端模块
- api/（路由层）：只负责请求/响应处理，不写业务逻辑
- services/（服务层）：业务逻辑，调用 core/ 和 models/
- core/（核心层）：AI 相关核心逻辑，不依赖 api/
- tools/（工具层）：各工具独立，互不依赖
- models/（数据层）：只定义数据结构，不写业务逻辑

---

## 8. 禁止事项

以下操作严格禁止，违反将导致代码被拒绝：

1. **禁止**在 .env 文件中提交真实 API Key
2. **禁止**将 LLM 调用逻辑写在 api/ 路由层，必须放在 core/ 或 services/
3. **禁止**跨模块直接调用（如 tools/ 调用 api/）
4. **禁止**在组件中直接调用 fetch/axios，必须通过 api/ 层
5. **禁止**硬编码 Agent 权限层级结构（必须从工作流 JSON 动态读取）
6. **禁止**在任务未完成时更新 PROGRESS.md 的已完成状态
7. **禁止**修改 AGENTS.md 中的任何内容
8. **禁止**在 main 分支直接提交代码
9. **禁止**忽略错误：所有异步操作必须有 try/catch
10. **禁止**提交未通过 Lint 检查的代码

---

## 9. 错误处理规范

- 所有 async 函数必须有 try/catch
- 后端 API 错误统一返回格式：`{"error": "描述", "code": "错误码"}`
- 前端所有请求错误必须有用户可见的提示
- Agent 执行错误必须记录到 loguru 日志
- 不允许 `pass` 空的 except 块

---

## 10. 安全规范

- 密码必须使用 bcrypt 加密，禁止明文存储
- 所有 API 接口（除登录注册外）必须通过 JWT 验证
- 文件路径操作必须做路径穿越检查
- 代码执行工具必须有超时限制（默认 30 秒）
- 用户上传文件必须做类型和大小校验
```

---

## 八、PROGRESS.md 进度文件模板

> 将以下内容保存为项目根目录的 `PROGRESS.md`，每次任务执行后更新。

---

```markdown
# PROGRESS.md — 开发进度记录

> 最后更新：[日期]
> 更新者：Codex

---

## 当前阶段

**阶段一：基础骨架 MVP**

---

## 阶段总览

| 阶段 | 状态 | 说明 |
|------|------|------|
| 阶段一：基础骨架 MVP | 🔄 进行中 | |
| 阶段二：工作流引擎 | ⏳ 待开始 | |
| 阶段三：工具集接入 | ⏳ 待开始 | |
| 阶段四：完善体验 | ⏳ 待开始 | |
| 阶段五：扩展能力 | ⏳ 待开始 | |

---

## 已完成模块

- [ ] 示例（完成后勾选并添加完成时间）

---

## 未完成模块（当前阶段）

- [ ] 项目脚手架搭建
- [ ] 用户注册/登录
- [ ] 基础对话界面（WebSocket 流式输出）
- [ ] LLM 适配层
- [ ] Manager Agent 基础对话
- [ ] SQLite 数据存储
- [ ] 左侧历史边栏
- [ ] 亮/暗主题切换

---

## 当前实际目录结构快照

```
（每次执行后由 Codex 更新为实际目录结构）
```

---

## 操作日志

### [日期] 会话 #1
- 执行内容：（描述本次做了什么）
- 创建文件：（列出新建的文件）
- 修改文件：（列出修改的文件）
- 遗留问题：（记录未解决的问题或下次需要注意的事项）

---

## 已知问题 / 待决定事项

（记录开发中发现的问题或需要用户决策的事项）
```

---

*文档由 Claude 生成 | 如需调整任何模块细节，随时告知*
